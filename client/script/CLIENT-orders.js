		var options = {
            valueNames: [ 'id_pedido'],
            page: 10,
            pagination:{
                outerWindow :1
            }
        };
        
        var userList = new List('ordersList', options);